{
  "name": "morse-typing-system",
  "version": "1.0.0",
  "description": "A single-key typing system that converts morse code input into readable text",
  "main": "src/js/modules/App.js",
  "type": "module",
  "scripts": {
    "test": "jest",
    "start": "python -m http.server 8000",
    "dev": "live-server --port=8000 --open=index.html",
    "build": "echo 'Build not implemented'"
  },
  "devDependencies": {
    "@babel/core": "^7.28.4",
    "@babel/preset-env": "^7.28.3",
    "babel-jest": "^30.1.2",
    "jest": "^29.7.0",
    "jest-environment-jsdom": "^29.7.0",
    "live-server": "^1.2.2"
  },
  "jest": {
    "testEnvironment": "jsdom",
    "transform": {
      "^.+\\.js$": "babel-jest"
    }
  }
}
